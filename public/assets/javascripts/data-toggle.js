// Generated by CoffeeScript 1.7.1
(function() {
  var toggle;

  toggle = function(element, first, second, active) {
    var _ref;
    if (active == null) {
      active = !element.hasClass(first);
    }
    if (!active) {
      _ref = [second, first], first = _ref[0], second = _ref[1];
    }
    element.addClass(first).removeClass(second);
    return active;
  };

  $(document).on("click", "[data-toggle]", function(event) {
    var open, selector, toggler;
    event.preventDefault();
    selector = $(this).data("toggle");
    toggler = $(selector);
    open = toggle(toggler, "toggle-open", "toggle-closed");
    return $("[data-toggle='" + selector + "']").each(function() {
      return toggle($(this), "toggle-open", "toggle-closed", open);
    });
  });

}).call(this);
